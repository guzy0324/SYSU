/* EXAMPLE 3: An Array and some Maths */
program ex3;
class ex {
        method void main()
	     declarations
                	int[] ia = int[101];
                	int i = 0; 
	                int sum = 0;
	     enddeclarations
	{	while (i < ia.length) {
                        ia[i] := i;
			i := i + 1;
		};
                i := 0; 
		while (i < ia.length) {
                        sum := sum + ia[i,i];
			i := i + 1;
		};
                System.println(sum);
        }
}

